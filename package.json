{
  "name": "sharing-plugin-example",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "cross-env NODE_ENV=development rspack serve",
    "build": "cross-env NODE_ENV=production rspack build",
    "start": "npm run dev"
  },
  "devDependencies": {
    "@rspack/cli": "^1.1.4",
    "@rspack/core": "^1.1.4",
    "cross-env": "^7.0.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@showpad/content-picker": "^0.1.0"
  }
}
